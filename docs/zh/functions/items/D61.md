# D61 向上螺旋自维持函数

[← 返回函数表 / Back to Functions](../../functions.md)
[返回仓库首页 / Back to Repository Home](../../../../README.md)

## 函数内容 / Function Content

中文：
沃尔顿的向上螺旋：干预→信心↑→尝试↑→正面结果→信心↑↑。D-X40只判定窗口内是否点火成功，没有描述点火成功后的自维持动力学。 dε_eff/dt|_{upward} = α_up × ε_eff(t) × (1 - ε_eff(t)/ε_opt) × (1 - P(biased_interpretation)) - α_up：向上螺旋速率 - ε_eff × (1-ε_eff/ε_opt)：logistic增长，ε_eff趋向ε_opt但不超调 - (1-P(biased))：偏置解读被抑制——向上螺旋的燃料是"不再偏置解读" 与D-X28的关系：D-X28给出P_internal = k×ε×exp(-ε/ε_opt)，是倒U型。D-X56是点火成功后ε_eff在logistic增长段的行为——ε_eff从窗口下界ε_aware_min向ε_opt趋近，不会超调到反弹区。

English:
沃尔顿的向上螺旋: 干预 -> 信心↑ -> 尝试↑ -> 正面结果 -> 信心↑↑. D-X40只判定窗口内是否Ignition成功, 没有描述Ignition成功后的自维持动力学. dε_eff/dt|_{upward} = α_up x ε_eff(t) x (1 - ε_eff(t)/ε_opt) x (1 - P(biased_interpretation)) - α_up: 向上螺旋速率 - ε_eff x (1-ε_eff/ε_opt): logistic增长, ε_eff趋向ε_opt但不超调 - (1-P(biased)): 偏置解读被抑制 - - 向上螺旋的燃料是"不再偏置解读" 与D-X28的关系: D-X28给出P_internal = k x ε x exp(-ε/ε_opt), 是倒U型. D-X56是Ignition成功后ε_eff在logistic增长段的行为 - - ε_eff从窗口下界ε_aware_min向ε_opt趋近, 不会超调到反弹区.

## 它解决的问题 / Problem It Addresses

中文：
该函数通过 沃尔顿的向上螺旋：干预→信心↑→尝试↑→正面结果→信心↑↑。D-X40只判定窗口内是否点火成功，没有描述点火成功后的自维持动力学。 dε_eff/dt|_{upward} = α_up × ε_eff(t) × (1 - ε_eff(t)/ε_opt) × (1 - P(biased_interpretation)) - α_up：向上螺旋速率 - ε_eff × (1-ε_eff/ε_opt)：logistic增长，ε_eff趋向ε_opt但不超调 - (1-P(biased))：偏置解读被抑制——向上螺旋的燃料是"不再偏置解读" 与D-X28的关系：D-X28给出P_internal = k×ε×exp(-ε/ε_opt)，是倒U型。D-X56是点火成功后ε_eff在logistic增长段的行为——ε_eff从窗口下界ε_aware_min向ε_opt趋近，不会超调到反弹区。 描述 向上螺旋自维持函数。

English:
该函数通过 沃尔顿的向上螺旋: 干预 -> 信心↑ -> 尝试↑ -> 正面结果 -> 信心↑↑. D-X40只判定窗口内是否Ignition成功, 没有描述Ignition成功后的自维持动力学. dε_eff/dt|_{upward} = α_up x ε_eff(t) x (1 - ε_eff(t)/ε_opt) x (1 - P(biased_interpretation)) - α_up: 向上螺旋速率 - ε_eff x (1-ε_eff/ε_opt): logistic增长, ε_eff趋向ε_opt但不超调 - (1-P(biased)): 偏置解读被抑制 - - 向上螺旋的燃料是"不再偏置解读" 与D-X28的关系: D-X28给出P_internal = k x ε x exp(-ε/ε_opt), 是倒U型. D-X56是Ignition成功后ε_eff在logistic增长段的行为 - - ε_eff从窗口下界ε_aware_min向ε_opt趋近, 不会超调到反弹区. 描述 向上螺旋自维持函数.

## 层级与状态 / Level and Status

- 层级 / Level：推论 / Derived function
- 状态 / Status：CROSS_VERIFIED / 交叉验证 / Cross-verified

## 关联案例 / Related Cases

- [沃尔顿10年追踪](../../cases/items/C-0155.md)
  中文：该案例在源表中引用了该函数。
  English: The source table links this case to the function.

## 来源回指 / Source Reference

- 源总表 / Source table：`data/functions/统一函数总表_470条_源文交叉重建版_v4.md`
- 源行 / Source line：120
- 来源笔记ID / Source note ID：`1912452451542949768`
- 来源文件 / Source file：`getnote:1912452451542949768`

## 源文片段 / Source Excerpt

> | 109 | D61 | 推论 | 向上螺旋自维持函数 | 沃尔顿的向上螺旋：干预→信心↑→尝试↑→正面结果→信心↑↑。D-X40只判定窗口内是否点火成功，没有描述点火成功后的自维持动力学。 dε_eff/dt\|_{upward} = α_up × ε_eff(t) × (1 - ε_eff(t)/ε_opt) × (1 - P(biased_interpretation)) - α_up：向上螺旋速率 - ε_eff × (1-ε_eff/ε_opt)：logistic增长，ε_eff趋向ε_opt但不超调 - (1-P(biased))：偏置解读被抑制——向上螺旋的燃料是"不再偏置解读" 与D-X28的关系：D-X28给出P_internal = k×ε×exp(-ε/ε_opt)，是倒U型。D-X56是点火成功后ε_eff在logistic增长段的行为——ε_eff从窗口下界ε_aware_min向ε_opt趋近，不会超调到反弹区。 | getnote:1912452451542949768:83 | derivation | CROSS_VERIFIED |
