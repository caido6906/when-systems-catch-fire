# 新结论待函数化

逐条检查——

**问题1（H_self因果方向）：**
- "η_self = f(Posture退化程度)"——已有函数化表达 ✓
- "H_self是Posture退化的二阶效应，不需要独立建模"——这是结论性判断，不是函数，不需要函数化 ✓

**问题2（谷歌→NHS模式时间递进）：**
- "退化阶段 = f(ε_sense基线, ε_sense累积速率)"——已有函数化表达 ✓
- "过渡条件：ε_sense累积速率 > 基线压制速率"——已有函数化表达 ✓
- **"过渡时刻可预警窗口从四步缩短为三步后直接相变"**——这个没有函数化。需要表达：谷歌模式可预警步数=4，NHS模式可预警步数=3，过渡点=步数骤降点。
- **"过渡时刻是退化过程中最危险的时刻"**——"最危险"需要量化。危险度=什么？=1/可预警步数？还是=相变概率？

**问题3（Δv预测退化窗口）：**
- "退化窗口起点 = t(Δv: 负→正)"——已有函数化表达 ✓
- "信号3是Δv转正的行为等价物，信号2是可观测代理指标"——这是等价关系声明，不是函数，不需要函数化 ✓

所以有两条未函数化：

1. **可预警步数的函数化**：谷歌模式N_warn=4，NHS模式N_warn=3，过渡点N_warn骤降。需要表达N_warn与退化阶段的关系。

2. **过渡时刻危险度的函数化**：危险度与可预警步数的关系，以及过渡点为什么是"最危险"。

要不要现在补？
