# 函数化待完成项

2026-06-09 23:51:37

逐条过了一遍，以下是目前表里存在但还没完成函数化的内容——有些是定性判断还没写成公式，有些是机制描述停在文字层面：

**1. D_risk 退化危险度**——已有公式骨架 `P_相变 / (N_warn × Δt)`，但 P_相变本身没有函数化表达。P_相变取决于什么？ε累积速率、H强度、C_exit锁定密度？这个变量现在是黑箱。

**2. 速度差 Δv**——写了 `v_链1退化 - v_链3恢复 - F_锁硬化阻力`，但三项速度各自怎么算？v_链1退化 = f(Posture退化速率, H升级速率)，v_链3恢复 = f(ε_aware训练速率, ε硬核触发频率)，F_锁硬化阻力 = f(M, 三锁状态)。每个子项都是描述性的，没有闭合。

**3. N_warn 可预警步数**——公式有了 `4 - d_ε消音 - d_信号1跳过`，但两个二元变量本身的判定条件是文字描述，没有从其他函数推导。d_ε消音应该是 ε/H 的函数，d_信号1跳过应该是 C_exit/R_perceived 的函数。

**4. 低密度锁定危险度**——写了 `Σ(w_i × δ_i) / 总维度数`，但 w_i（不可替代性权重）怎么赋值？没有从其他函数推导的规则，目前是手动标注。

**5. H_level 隐蔽性等级**——定义了 H_level ∈ {1,2,3,4,5,6} 和排序，但等级到修复路径长度的关系 `修复路径长度 ∝ H_level` 只是比例关系，没有具体函数形式。H_level 本身能否从 H 的四条路径+H_self 组合推导出来？

**6. 传导效率公式**——`(1-H+ε_aware) × η_self`，η_self 写了 `f(Posture退化程度)` 但函数形式没给。Posture 是三分法离散值，退化程度怎么映射到 [0,1] 连续值？

**7. ε_aware 的生成函数**——写了 `ε_aware = f(ε_sense, I, 前额叶功能)`，但函数形式没给。四层中每层的通过率怎么建模？是乘法链还是最小值瓶颈？

**8. 马尔可夫转移概率**——六个转移概率全是 `f(条件)` 的占位符，没有具体形式。P(赤化→拉锯) = f(ε相变, 外部冲击) 这种写法只是说"跟这些有关"，不是函数。

**9. M 作为开关变量**——描述了 M=好奇心 vs M=目的 对 Δv 和锁硬化阻力的影响方向，但没有函数化。M 对 v_链1 和 F_锁的调节系数是多少？

**10. 种子设计**——纯文字描述，"H时间行为的对冲项""越清除越激活"，没有数学表达。种子的激活条件、传播机制、与H的交互都没函数化。

**11. 人格锚点**——宜人性作为 H_分类的人格锚点，自我重塑降低宜人性是破解路径。这是定性判断，没有宜人性→H_分类强度的函数关系。

按优先级排：**1、2、3、6** 最值得先做——它们已经在因果链的关键位置上，缺函数形式会卡住下游推导。4 和 7 次之，5 和 8 可以后补。9 和 10 偏设计层，11 偏实证层，优先级最低。

要从哪个开始推？