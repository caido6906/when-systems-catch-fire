# 函数表更新比对

三张表全拉完了，逐项比对今天新增的函数化结果。

**需要更新的地方：**

**八函数总表：**
1. F1 I(t) 传导效率公式里，D_unified应该补进去——当前E_I = (1-H+ε_aware) × (1-Posture_deg^β)，但缺少解码效率D这个因子。完整传导应该是B×D×(1-H+ε_aware)×(1-Posture_deg^β)，D_unified是B×D里的D
2. N_warn节缺少认知信道统一函数Ψ和茧房破壁概率P_break的交叉引用
3. 系统走向监测缺少上下文处理架构函数Q(C,A)的引用
4. 更新记录需补第三轮

**认知函数表：**
1. C2 F_form缺少专业化遮蔽函数H_pro的交叉引用
2. C5 L_five缺少D_unified在层间跳变中的角色标注——σ项里的I·ε·H实际是B×D_unified×ε×(1-H)，D_unified是底层机制
3. 更新记录需补第三轮

**函数验证表：**
1. 缺少链5（上下文架构）、链6（认知信道）、链7（认知遮蔽）三个场景
2. 缺少D_unified统一解码效率函数
3. 缺少六条链统一收敛对照表
4. 缺少ε是全系统相变参数的标注

现在逐表更新。三张表全部更新完，存进点火知识库了。更新要点：

**八函数总表**——F1传导效率补了D_unified（B×D才是有效信号），F7补了H_pro专业化遮蔽交叉引用，F8补了ε是全系统相变参数的标注，系统走向监测新增Q(C,A)上下文架构函数和Ψ(L,t)认知信道函数

**认知函数表**——C2补了专业化遮蔽对F_form的效应函数，C3补了专业化遮蔽对M_cog的效应，θ₃跳变条件更新为B×D_unified×ε×(1-H) > θ₃(d)

**函数验证表**——从4场景扩展到7场景（新增上下文架构、认知信道、认知遮蔽），新增D_unified统一解码效率函数，新增六条链统一收敛对照表，σ项底层机制更新为B×D_unified
