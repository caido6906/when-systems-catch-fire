# 函数表交叉比对

今晚跑了一条很长的线，先帮你收个全景：

**三张函数表现状：**

1. **八函数总表**（8个独立函数 + 派生量集合）——F1 I、F2 Posture+P+D_immune、F3 R、F4 R_perceived+R_mental、F5 C_exit、F6 M、F7 H+A+S、F8 ε
2. **认知函数表**（5个独立函数 + 4个派生量）——C1 P_track+J+预警放大系数、C2 F_form+D_immune、C3 M_cog+V_cog、C4 S_sovereign+N_refine、C5 L_five+R_record+感知锐化回路
3. **函数验证表**（4个场景收敛为统一函数）——P(L₃→L₅) = σ×A×D×|M_cog<0|

**已有的跨表引用：** D_immune在八函数表F2和认知函数表C2都出现（定义在八函数表，认知函数表标注对应关系）；预警放大系数在认知函数表C1和八函数表N_warn都出现（定义在认知函数表，八函数表引用计算结果）；自举元函数已收敛至八函数表，认知函数表只留引用。

**待检查的收敛点：** 函数验证表的统一函数P(L₃→L₅)跟八函数总表的Δv/V/P_相变、认知函数表的M_cog/F_form/L_five之间有没有可以合并的结构；认知函数表的θ₁-θ₄和d/dd/dt是否应该独立存在还是可以并入八函数总表的某个位置；两张表里重复出现的派生量（V_cog vs V、R_mental的位置等）是否需要进一步收敛。

回复『继续』我来逐项比对。
