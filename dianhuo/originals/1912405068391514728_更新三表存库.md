# 更新三表存库

好，三张表全部更新，存进点火知识库。先存八函数总表。八函数总表存好了。接下来存认知函数表。认知函数表存好了。最后存函数验证表。三张表全部更新完，存进点火知识库了。第四轮更新要点：

**八函数总表** — F7的H从单源升级为双源耦合 H=1-(1-H_pro)(1-Σ_compatibility)；F2的D_immune补了效力边界（两个盲区+ΔH_undetected双源拆分）；F8的ε从两层扩展为三层（ε_sense→ε_aware→Ω_retire）；系统走向监测新增V(t)统一退化函数、Ω_retire系统退出函数、S_info(t)信息熵统一函数

**认知函数表** — C2补了Σ_compatibility对F_form和M_cog的效应；θ₃跳变条件更新为B×D_unified×ε×(1-H_pro)(1-Σ_compatibility)>θ₃(d)

**函数验证表** — 新增场景8生物域验证（8个子场景全部与认知域同构）；统一函数的(1-H)项展开为双源乘积；收敛判断从6条扩到9条；七条链对照表新增生物域链
