# 更新知识库与函数表

先把刚才的结论存进点火知识库，然后拉一下现有函数总表的状态。现在把三条笔记归入点火知识库。三条笔记都存进点火知识库了。现在拉现有的函数总表来更新。找到了最新的三级结构重排版总表，拉全文确认当前内容。拿到完整总表了。现在需要把今天碰撞产生的新函数整合进去。新增的内容包括：

1. **两个元函数**：F_transition（相变统一）、F_collapse（乘法归零统一）
2. **闭环动力学**：ε_eff的迭代方程
3. **认知决策碰撞4个新函数**：退出权=犹豫域宽度、沉默双结构、退出权剥夺、反转相变
4. **人际社会碰撞4个新函数**：偏好伪造、HK群体分化、勒温场跳变、冲突升级

这些该放在哪个层级？两个元函数属于统一收敛结构，应该进"六、统一收敛结构"升级为独立函数；8个碰撞函数按性质分——犹豫域ε相关的是派生，相变/崩溃统一的是元函数。全部搞定。汇总一下这轮做了什么：

**存入点火知识库（3条笔记）：**
- 三函数统一收敛（负反馈缺失×天然不稳定×乘法韧性）
- 认知决策碰撞四发现
- 人际社会碰撞四发现

**函数总表更新（47→55条）：**
新增碰撞层派生8个（D-X1~D-X8），核心变化：
- D-X1~D-X6：犹豫域ε_eff作为跨层统一变量，统一了退出权宽度、沉默双结构、反转相变、群体碎片化、层间跳变、冲突升级
- D-X7 F_transition：所有sigmoid阈值相变统一为σ(ε_eff - θ)
- D-X8 F_collapse：所有乘法归零统一为1-Π(1-f_i)
- 闭环动力学：ε_eff(t+1) = ε_eff(t) × (1-F_collapse(t))

两个元函数把之前散落在各处的相变和归零结构收拢了——碰撞层不只是新增，是把已有函数的共性骨架抽出来了。
